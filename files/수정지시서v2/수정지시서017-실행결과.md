# 수정지시서 017 실행결과: .ssac 확장자 도입 + 패키지 모델 파라미터 매칭 검증

## 실행일: 2026-03-11

## 변경 내용

### 1. .ssac 확장자 도입

**parser/parser.go**

`ParseDir()` 파일 필터 `.go` → `.ssac`:

```go
func ParseDir(dir string) ([]ServiceFunc, error) {
    // ...
    if err != nil || d.IsDir() || !strings.HasSuffix(d.Name(), ".ssac") {
        return err
    }
```

**generator/generator.go**

출력 파일명 생성 시 `.go` → `.ssac` TrimSuffix:

```go
outName := strings.TrimSuffix(sf.FileName, ".ssac") + ext
```

**specs/dummy-study/service/**

7개 서비스 파일 확장자 변경:
- `auth/login.go` → `auth/login.ssac`
- `reservation/cancel_reservation.go` → `reservation/cancel_reservation.ssac`
- `reservation/create_reservation.go` → `reservation/create_reservation.ssac`
- `reservation/get_reservation.go` → `reservation/get_reservation.ssac`
- `reservation/list_my_reservations.go` → `reservation/list_my_reservations.ssac`
- `room/delete_room.go` → `room/delete_room.ssac`
- `room/update_room.go` → `room/update_room.ssac`

**변경하지 않은 곳:**
- `validator/symbol.go` — `loadGoInterfaces()`, `loadPackageGoInterfaces()`: model/ 디렉토리의 Go interface 파일이므로 `.go` 유지
- `generator/go_target.go` — `FileExtension()`: 생성 코드는 `.go`
- `v1/testdata/` — 아카이브, 수정 금지

### 2. MethodInfo 확장

**validator/symbol.go**

`MethodInfo`에 `Params []string` 필드 추가:

```go
type MethodInfo struct {
    Cardinality string   // "one", "many", "exec"
    Params      []string // interface 파라미터명 (context.Context 제외, 패키지 모델용)
}
```

### 3. loadPackageGoInterfaces — 파라미터 수집

**validator/symbol.go**

interface 메서드에서 파라미터명 수집 (`context.Context` 제외):

```go
var params []string
if ft, ok := method.Type.(*ast.FuncType); ok && ft.Params != nil {
    for _, param := range ft.Params.List {
        if isContextType(param.Type) {
            continue
        }
        for _, name := range param.Names {
            params = append(params, name.Name)
        }
    }
}
ms.Methods[method.Names[0].Name] = MethodInfo{Params: params}
```

`isContextType()` 헬퍼 추가:

```go
func isContextType(expr ast.Expr) bool {
    sel, ok := expr.(*ast.SelectorExpr)
    if !ok { return false }
    ident, ok := sel.X.(*ast.Ident)
    if !ok { return false }
    return ident.Name == "context" && sel.Sel.Name == "Context"
}
```

### 4. validateModel — 파라미터 매칭 검증

**validator/validator.go**

패키지 모델 분기에서 메서드 존재 확인 후 파라미터 양방향 비교:

```go
mi := ms.Methods[methodName]
if len(mi.Params) > 0 {
    // SSaC에 있지만 interface에 없는 키 → ERROR
    // interface에 있지만 SSaC에 없는 파라미터 → ERROR
}
```

ERROR 메시지 형식:
```
[ERROR] SSaC ↔ Interface: GetSession — @model session.Session.Get 파라미터 불일치. SSaC에 "token"가 있지만 interface에 없습니다. interface 파라미터: [key]
[ERROR] SSaC ↔ Interface: CreateSession — @model session.Session.Set 파라미터 누락. interface에 "ttl"가 필요하지만 SSaC에 없습니다. SSaC 파라미터: [key, value]
```

## 테스트 추가

| 테스트 | 검증 내용 |
|---|---|
| `TestParseFlatServiceError` (수정) | `.ssac` 파일로 flat 에러 검증 |
| `TestParseDomainFolder` (수정) | `.ssac` 파일로 도메인 폴더 파싱 |
| `TestValidatePackageParamMatch` | SSaC 파라미터 = interface 파라미터 → OK |
| `TestValidatePackageParamExtra` | SSaC에 interface에 없는 파라미터 → ERROR |
| `TestValidatePackageParamMissing` | interface에 있지만 SSaC에 없는 파라미터 → ERROR |
| `TestValidatePackageParamSkipContext` | `context.Context` 파라미터 제외 확인 |

## 테스트

```
ok  github.com/geul-org/ssac/parser     0.010s
ok  github.com/geul-org/ssac/validator   0.002s
ok  github.com/geul-org/ssac/generator   0.005s
```

122 tests: parser 34 + validator 58 + generator 30

## 상태: 완료
