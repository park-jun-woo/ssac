# 수정지시서 009 실행결과: @call 반환 타입에 기본 타입 사용 시 validate 에러

## 실행일: 2026-03-10

## 변경 내용

### validator/validator.go

`isPrimitiveType()` 함수와 `primitiveTypes` set 추가:

```go
var primitiveTypes = map[string]bool{
    "string": true, "int": true, "int32": true, "int64": true,
    "float32": true, "float64": true, "bool": true, "byte": true,
    "rune": true, "time.Time": true,
}

func isPrimitiveType(s string) bool {
    return primitiveTypes[s]
}
```

`validateRequiredFields()`에서 `@call` result 타입이 기본 타입이면 ERROR:

```go
case parser.SeqCall:
    if seq.Result != nil && isPrimitiveType(seq.Result.Type) {
        errs = append(errs, ctx.err("@call",
            fmt.Sprintf("반환 타입에 기본 타입 %q은 사용할 수 없습니다 — Response struct 타입을 지정하세요", seq.Result.Type)))
    }
```

### validator/validator_test.go

- `TestValidateCallPrimitiveReturnType`: string, int, int64, bool, float64, time.Time 모두 에러 확인
- `TestValidateCallStructReturnTypeOK`: struct 타입은 에러 없음
- `TestValidateCallNoResultOK`: result 없는 @call은 에러 없음

## 테스트

```
ok  github.com/geul-org/ssac/parser     0.007s
ok  github.com/geul-org/ssac/validator   0.002s
ok  github.com/geul-org/ssac/generator   0.003s
```

## 상태: 완료
