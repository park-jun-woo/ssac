# 수정지시서 021 실행결과: @response 단일 행 구조체 형식 파싱 지원

## 실행일: 2026-03-11

## 변경 내용

### parser/parser.go

`parseLine()`의 @response 분기에서 `trimmed == "{"` (멀티라인 시작)과 shorthand (변수명) 사이에 단일 행 구조체 분기 추가:

```go
// 단일 행 구조체: @response { field: var, ... }
if strings.HasPrefix(trimmed, "{") && strings.HasSuffix(trimmed, "}") {
    inner := trimmed[1 : len(trimmed)-1]
    lines := strings.Split(inner, ",")
    return &Sequence{
        Type:         SeqResponse,
        Fields:       parseResponseFields(lines),
        SuppressWarn: suppressWarn,
    }, false, nil
}
```

기존 `parseResponseFields([]string)` 그대로 재사용. 쉼표로 split한 결과를 넘기면 `key: value` 형식을 파싱.

## 테스트 추가

### Parser (2개)

| 테스트 | 검증 내용 |
|---|---|
| `TestParseResponseSingleLine` | `@response { user: user }` → Fields 1개, Target 비어있음 |
| `TestParseResponseSingleLineMultiFields` | `@response { user: user, name: user.Name }` → Fields 2개 |

## 테스트

```
ok  github.com/geul-org/ssac/parser     0.013s
ok  github.com/geul-org/ssac/validator   0.003s
ok  github.com/geul-org/ssac/generator   0.005s
```

148 tests: parser 41 + validator 72 + generator 35

## 상태: 완료
