# 수정지시서 026 실행결과: @call 에러 HTTP 상태 코드 커스텀 지정 + 기본값 500

## 실행일: 2026-03-11

## 변경 내용

### parser/types.go — ErrStatus 필드 추가

```go
// 공통
Message      string // 에러 메시지
ErrStatus    int    // @call 에러 HTTP 상태 코드 (0이면 기본값 500)
SuppressWarn bool   // @type! — WARNING 억제
```

### parser/parser.go

#### parseCallExprInputs — remainder 반환 추가

```go
// 변경 전
func parseCallExprInputs(expr string) (string, map[string]string, error)

// 변경 후
func parseCallExprInputs(expr string) (string, map[string]string, string, error)
```

마지막 `)` 뒤의 remainder를 4번째 반환값으로 전달.

#### parseCall — trailing HTTP status 파싱

```go
// trailing HTTP status code (e.g. "401")
if remainder != "" {
    if code, err := strconv.Atoi(remainder); err == nil && code >= 100 && code <= 599 {
        seq.ErrStatus = code
    }
}
```

#### parseCRUD — 호출부 4-value 대응

`parseCRUD`에서도 `parseCallExprInputs`를 사용하므로 `_, err` → `_, _, err` 패턴으로 remainder 무시.

### generator/go_target.go

#### templateData에 ErrStatus 추가

```go
ErrStatus string // "http.StatusInternalServerError", "http.StatusUnauthorized" 등
```

#### buildTemplateData — @call ErrStatus 결정

```go
if seq.ErrStatus != 0 {
    d.ErrStatus = httpStatusConst(seq.ErrStatus)
} else {
    d.ErrStatus = "http.StatusInternalServerError"
}
```

#### httpStatusConst 헬퍼 추가

```go
func httpStatusConst(code int) string {
    switch code {
    case 400: return "http.StatusBadRequest"
    case 401: return "http.StatusUnauthorized"
    case 402: return "http.StatusPaymentRequired"
    case 403: return "http.StatusForbidden"
    case 404: return "http.StatusNotFound"
    case 409: return "http.StatusConflict"
    case 429: return "http.StatusTooManyRequests"
    case 500: return "http.StatusInternalServerError"
    case 502: return "http.StatusBadGateway"
    case 503: return "http.StatusServiceUnavailable"
    default:  return fmt.Sprintf("%d", code)
    }
}
```

### generator/go_templates.go — 템플릿 수정

#### call_no_result

```
// 변경 전
c.JSON(http.StatusUnauthorized, gin.H{"error": "{{.Message}}"})

// 변경 후
c.JSON({{.ErrStatus}}, gin.H{"error": "{{.Message}}"})
```

#### call_with_result

```
// 변경 전
c.JSON(http.StatusInternalServerError, gin.H{"error": "{{.Message}}"})

// 변경 후
c.JSON({{.ErrStatus}}, gin.H{"error": "{{.Message}}"})
```

`sub_call_no_result`, `sub_call_with_result`는 `return fmt.Errorf(...)` — HTTP 상태 코드 없으므로 변경 불필요.

## 테스트 변경

### 추가 (3개)

| 테스트 | 패키지 | 검증 내용 |
|---|---|---|
| `TestParseCallErrStatus` | parser | `@call auth.Verify({...}) 401` → `ErrStatus: 401` |
| `TestParseCallErrStatusWithResult` | parser | `@call Type var = pkg.Func({...}) 402` → `ErrStatus: 402` |
| `TestGenerateCallErrStatus` | generator | `ErrStatus: 401` → `http.StatusUnauthorized` 생성 확인 |

### 기존 테스트 수정 (1개)

| 테스트 | 변경 |
|---|---|
| `TestGenerateCallWithoutResult` | `StatusUnauthorized` → `StatusInternalServerError` (기본값 500 통일) |

## 테스트

```
ok  github.com/geul-org/ssac/parser     0.012s
ok  github.com/geul-org/ssac/validator   0.002s
ok  github.com/geul-org/ssac/generator   0.007s
```

163 tests: parser 43 + validator 75 + generator 45

## 상태: 완료
