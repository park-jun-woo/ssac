# 수정지시서 015 실행결과: @response 간단쓰기 시 불필요한 pagination import 제거

## 실행일: 2026-03-11

## 변경 내용

### generator/go_target.go

`collectImports()`에서 `@response` 간단쓰기(Target 설정) 여부 확인. 간단쓰기면 handler에서 pagination 타입을 직접 참조하지 않으므로 import 스킵:

```go
if seq.Result != nil && seq.Result.Wrapper != "" {
    hasDirectResponse := false
    for _, s := range sf.Sequences {
        if s.Type == parser.SeqResponse && s.Target != "" {
            hasDirectResponse = true
            break
        }
    }
    if !hasDirectResponse {
        seen["github.com/geul-org/fullend/pkg/pagination"] = true
    }
}
```

### generator/generator_test.go

`TestGenerateResponseDirect`에 `assertNotContains(t, code, "pagination")` 추가.

## 테스트

```
ok  github.com/geul-org/ssac/parser     0.009s
ok  github.com/geul-org/ssac/validator   0.002s
ok  github.com/geul-org/ssac/generator   0.005s
```

## 상태: 완료
