# 수정지시서 010 실행결과: validateResponse 및 ResponseFields 제거

## 실행일: 2026-03-13

## 방침

`@response` ↔ OpenAPI response 교차 검증은 fullend 소관. SSaC에서 완전 제거.

## 변경 내용

### 1. `validator/validator.go` — 함수 3개 삭제

- `validateResponse` 함수 전체 삭제 (약 65행)
- `resolveWrapperFields` 헬퍼 삭제
- `wrapperFieldsMap` 변수 삭제
- `ValidateWithSymbols`에서 `validateResponse` 호출 제거

### 2. `validator/symbol.go` — ResponseFields 제거

- `OperationSymbol` struct에서 `ResponseFields map[string]bool` 필드 삭제
- `loadOpenAPI`에서 response fields 수집 코드 삭제 (200 응답 스키마 파싱)
- `OperationSymbol` 초기화에서 `ResponseFields: make(map[string]bool)` 삭제

### 3. `validator/validator_test.go` — 테스트 5개 삭제

- `TestValidateResponseFieldNotInOpenAPI` — 삭제
- `TestValidateReverseResponseMissing` — 삭제
- `TestValidateReverseResponsePaginationTotal` — 삭제
- `TestValidateResponseDirectPageFieldsMatch` — 삭제
- `TestValidateResponseDirectPageFieldsMissing` — 삭제
- 나머지 테스트에서 `ResponseFields` 필드 참조 제거

### 4. `generator/go_handler_test.go` — ResponseFields 참조 제거

- `TestGeneratePathParam` OperationSymbol에서 `ResponseFields` 필드 제거

## 변경 파일

| 파일 | 변경 |
|---|---|
| `validator/validator.go` | `validateResponse`, `resolveWrapperFields`, `wrapperFieldsMap` 삭제, 호출 제거 |
| `validator/symbol.go` | `ResponseFields` 필드 + 로딩 코드 삭제 |
| `validator/validator_test.go` | 테스트 5개 삭제 + ResponseFields 참조 제거 |
| `generator/go_handler_test.go` | ResponseFields 참조 제거 |

## 영향 없는 범위

- `validateRequest` (요청 필드 검증) — 유지
- `validateModel` (모델 존재 검증) — 유지
- `validateQueryUsage`, `validatePaginationType`, `validateCallInputTypes` — 유지
- Guard 에러 코드 (`@empty`, `@exists`, `@state`, `@auth`) — 유지

## 테스트

```
ok  github.com/geul-org/ssac/parser     0.015s
ok  github.com/geul-org/ssac/validator   0.003s
ok  github.com/geul-org/ssac/generator   0.007s
```

전체 통과.

## 상태: 완료
