# 수정지시서 012 실행결과: 모델 필드 역참조(`*`) 원복

## 실행일: 2026-03-10

## 변경 내용

수정지시서010(`104b5ae`)을 `git revert`로 원복 → `e5d91a3`.

### generator/go_target.go

`inputValueToCode()`에서 `"*" + val` 역참조 로직 제거. `callResultVars` 파라미터 및 수집 로직도 함께 제거:

```go
func inputValueToCode(val string) string {
    if val == "query" { return "opts" }
    if strings.HasPrefix(val, "request.") { return lcFirst(val[len("request."):]) }
    // currentUser.Field, config.Field, 일반 변수 → 그대로
    return val
}
```

`buildArgsCodeFromInputs()`, `buildInputFieldsFromMap()`, `buildTemplateData()` — `callResultVars` 파라미터 제거.

### generator/generator_test.go

`*` 역참조 관련 assertion 원복:
- `*course.ID` → `course.ID`
- `*reservation.ID` → `reservation.ID`
- `*reservation.Status` → `reservation.Status`
- `*project.ID` → `project.ID`
- `*project.OwnerID` → `project.OwnerID`
- `*user.Email` → `user.Email`

### 삭제된 파일

- `specs/plans/Phase015-nullable-deref.md`
- `files/수정지시서v2/수정지시서010-실행결과.md`

## 테스트

```
ok  github.com/park-jun-woo/ssac/parser     0.007s
ok  github.com/park-jun-woo/ssac/validator   0.002s
ok  github.com/park-jun-woo/ssac/generator   0.003s
```

## 상태: 완료
