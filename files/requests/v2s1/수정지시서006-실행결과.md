# 수정지시서 006 실행결과: service/ 직접 .go 파일 금지 — 도메인 서브 폴더 강제

## 실행일: 2026-03-10

## 변경 내용

### parser/parser.go — `ParseDir()`

`filepath.Dir(rel) == "."` 검사 추가. service/ 직접에 .go 파일이 있으면 에러 반환.

```go
// 변경 전
for i := range sfs {
    rel, _ := filepath.Rel(dir, path)
    if parts := strings.Split(filepath.Dir(rel), string(filepath.Separator)); parts[0] != "." {
        sfs[i].Domain = parts[0]
    }
    funcs = append(funcs, sfs[i])
}

// 변경 후
rel, _ := filepath.Rel(dir, path)
if filepath.Dir(rel) == "." {
    return fmt.Errorf("%s — service/ 직접에 SSaC 파일을 둘 수 없습니다. 도메인 서브 폴더를 사용하세요 (예: service/auth/%s)", d.Name(), d.Name())
}
for i := range sfs {
    parts := strings.Split(filepath.Dir(rel), string(filepath.Separator))
    sfs[i].Domain = parts[0]
    funcs = append(funcs, sfs[i])
}
```

- `Domain == ""` 조건 분기 제거. 항상 `parts[0]` 설정.

### parser/parser_test.go

`TestParseFlatServiceError` 추가: flat 구조 파일이 에러를 반환하는지 검증.

### specs/dummy-study/service/ 도메인 폴더 마이그레이션

```
service/auth/login.go
service/reservation/cancel_reservation.go
service/reservation/create_reservation.go
service/reservation/get_reservation.go
service/reservation/list_my_reservations.go
service/room/delete_room.go
service/room/update_room.go
```

## 코드젠 검증

```
generated model interfaces in /tmp/ssac-phase006-check//model
generated 7 files in /tmp/ssac-phase006-check/

auth/login.go
reservation/cancel_reservation.go
reservation/create_reservation.go
reservation/get_reservation.go
reservation/list_my_reservations.go
room/delete_room.go
room/update_room.go
```

## 테스트

```
ok  github.com/park-jun-woo/ssac/parser     0.008s
ok  github.com/park-jun-woo/ssac/validator   0.002s
ok  github.com/park-jun-woo/ssac/generator   0.004s
```

## 상태: 완료
