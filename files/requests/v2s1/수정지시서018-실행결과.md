# 수정지시서 018 실행결과: Go 예약어 파라미터명 검증

## 실행일: 2026-03-11

## 변경 내용

### 1. Go 예약어 목록

**validator/validator.go**

Go 25개 예약어 맵 추가:

```go
var goReservedWords = map[string]bool{
    "break": true, "case": true, "chan": true, "const": true,
    "continue": true, "default": true, "defer": true, "else": true,
    "fallthrough": true, "for": true, "func": true, "go": true,
    "goto": true, "if": true, "import": true, "interface": true,
    "map": true, "package": true, "range": true, "return": true,
    "select": true, "struct": true, "switch": true, "type": true,
    "var": true,
}
```

### 2. validateGoReservedWords 함수

**validator/validator.go**

`ValidateWithSymbols()`에서 호출. SSaC Inputs 키를 `toLowerFirst()` 변환 후 Go 예약어 여부 확인:

```go
func validateGoReservedWords(funcs []parser.ServiceFunc, st *SymbolTable) []ValidationError {
    // 패키지 모델, @call은 models_gen.go 대상 아니므로 스킵
    // Inputs 키 → toLowerFirst() → 예약어 체크
    // DDL 테이블에서 컬럼 역추적 → 테이블명 포함 에러 메시지
}
```

- `toLowerFirst()`: 첫 글자 소문자 변환 (generator의 `lcFirst`와 동일 효과, 순환 참조 회피)
- `toSnakeCase()`: PascalCase → snake_case 변환 (generator와 동일 로직, validator 패키지 내 복제)
- `findColumnTable()`: snake_case 컬럼명으로 DDL 테이블 역추적

### 3. ERROR 메시지 형식

DDL 테이블 특정 가능:
```
ERROR: DDL column "type" in table "transactions" is a Go reserved word — rename the column (e.g. "tx_type")
```

DDL 테이블 특정 불가:
```
ERROR: parameter name "type" is a Go reserved word — rename the DDL column
```

### 4. 적용 범위

- DDL 모델 (접두사 없음)의 CRUD 시퀀스만 검증
- 패키지 모델 (`seq.Package != ""`)과 `@call`은 `models_gen.go` 대상 아니므로 스킵
- 중복 에러 방지: 동일 테이블.컬럼 조합은 1회만 출력

## 테스트 추가

| 테스트 | 검증 내용 |
|---|---|
| `TestValidateGoReservedWordInInputs` | `{type: request.Type}` + DDL "type" 컬럼 → ERROR |
| `TestValidateGoReservedWordNoConflict` | `{txType: request.TxType}` → OK |
| `TestValidateGoReservedWordRange` | `{range: request.Range}` → ERROR |

## 테스트

```
ok  github.com/park-jun-woo/ssac/parser     0.011s
ok  github.com/park-jun-woo/ssac/validator   0.002s
ok  github.com/park-jun-woo/ssac/generator   0.006s
```

125 tests: parser 34 + validator 61 + generator 30

## 상태: 완료
